struct Point {
    x: i64,
    y: i64,
}

impl Point {
    fn new(x: i64, y: i64) -> i64 {
        return x + y;
    }

    fn sum(self: i64, other: i64) -> i64 {
        return self + other;
    }
}

fn main() -> i64 {
    let result: i64 = Point::new(3, 4);
    return result;
}
