// Test: fib + while, no asm
fn fib(n: i32) -> i32 {
    if (n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

pub fn main() -> i32 {
    let x = fib(10);
    let mut counter = 0;

    while (counter < 100) {
        counter = counter + 1;
    }

    return x;
}
